import { KarolShopValidators } from './karol-shop-validators';

describe('KarolShopValidators', () => {
  it('should create an instance', () => {
    expect(new KarolShopValidators()).toBeTruthy();
  });
});
